const express = require('express');
const router = express.Router();
//models
const Course = require('../models/Course');

// şimdi bu router direkt homepagede mi yoksa localhostta mı?

  module.exports = router;